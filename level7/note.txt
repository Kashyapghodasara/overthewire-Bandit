===Level 7===

-> The password for the next level is stored in the file data.txt next to 
   the word millionth

- Command Used => cat, grep

- New Learning => 

- Here "grep" is main command, If we directly read the file - out cmd might be stucked !
- Because there are lots of data in file that's why we used grep "millionth".
