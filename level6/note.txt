===Level 6===

->The password for the next level is stored somewhere on the server and has all of the 
following properties:

    owned by user bandit7
    owned by group bandit6
    33 bytes in size

- Command Used => cat, find 

- New Learning => 

- Main and only command we used is "Find" - through find command we can find any file 
with specific user, group, permission, size, name etc. 
- This is very powerfull command if we used it properly.

- This is kind of complex level. I have to check the solution and then I get it, it is
not hard as I thought and I tried.

- Command is = find / -user bandit7 -group bandit6 -size 33c -type f 2>/dev/null

# -type f => It search for only files like documents or images, Ignores directory and 
#            symlinks

# 2>/dev/null => It redirect all the error to /null folder which directly execute to 
#                nothingness like blackhole and errors like Persmission Denied.
#                Which is very usefull when I understand it.
