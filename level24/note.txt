===Level 24===

-> A daemon is listening on port 30002 and will give you the password for bandit25 if given the password for bandit24 and a secret numeric 4-digit pincode. There is no way to retrieve the pincode except by going through all of the 10000 combinations, called brute-forcing.
You do not need to create new connections each time.

- Command Used => bash script only

- New Learning =>

- They already told us we have to do brute force to find the password on localhost:30002.
- The only things works on brute force is "for loop"

# for i in $(seq -w 0000 9999); do echo "hVQMk3lJNsmQ7VF3ubyrNNBom7BOgVXv $i"; done | nc localhost 30002

- It's simple one line script - There is a pipe operation which sends password + pincode to nc command.

- I created a script which is nearly closer which is less efficient and slow
