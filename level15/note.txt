===Level 15===

->The password for the next level can be retrieved by submitting the password of the current level to port 30001 on localhost using SSL/TLS encryption.

Helpful note: Getting “DONE”, “RENEGOTIATING” or “KEYUPDATE”? Read the “CONNECTED COMMANDS” section in the manpage.

- Command Used => openssl s_client

- New Learning =>

- Here we used SSL/TLS protocol for secure communication which provide encryption/decryption and data integrity.

- It uses Certificate for authorization.
- HTTPS and SMPTS

- Meanwhile "nc & telnet" uses non-encrypted channel for data communcation which sent and recieved raw data.

- It uses SSH key for authorization
- HTTP and SMPT
