===Level 16===

-> The credentials for the next level can be retrieved by submitting the password of the current level to a port on localhost in the range 31000 to 32000. First find out which of these ports have a server listening on them. Then find out which of those speak SSL/TLS and which don’t. There is only 1 server that will give the next credentials, the others will simply send back to you whatever you send to it.

- Command Used => openssl s_client, nmap, ncat --ssl

- New Learning =>

- There is lot of things to learn. First start with Nmap - which helps to find open ports from 31000 to 32000 range. There are five open ports. Among them there are 2port which activve as SSL/TLS

- Then I have to find which port uses SSL/TLS - to find this we have 2 main option "openssl" or "ncat" (ncat is much more easy then openssl but I don't know that something like ncat also exist)

- I go with openssl and I did everything right but at the end when server asked for password went wrong, everytime.
- When I paste password it said KEYUPDATE - I searched everywhere what is this and how to solve this but I couldn't find anything then I went to chatgpt (Not to find the answer!) and it said don't press enter after pasted the password. It worked for one port but it is not the final port so tried into another and it hadn't worked !! 

- I was readlly frustating then I find something like "-ign_eof" in manpages. They say we have to add this into openssl command it bypass unwanted connection termination and keep it stable and after pasting password you will get RSA key. 

# openssl s_client -ign_eof localhost:31790
# ncat --ssl locahost 31790
